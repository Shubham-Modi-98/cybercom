@using PassDatafromViewtoController.Models
@model IEnumerable<PassDatafromViewtoController.Models.Student>


@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>DisplayData</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Index")
    </p>
    <h1>Record Inserted Successfully</h1>
    <br />
    <hr />
    <div>
        @Html.ActionLink("View List", "Display")
    </div>
    @*<table class="table">
            <tr>
                <th>
                    @Html.DisplayName("First Name")
                </th>
                <th>
                    @Html.DisplayName("Last Name")
                </th>
                <th>
                    @Html.DisplayName("Email")
                </th>
                <th>
                    @Html.DisplayName("Mobile Number")
                </th>
                <th></th>
            </tr>

        @foreach (var item in Model as List<Student>) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Mobile)
                </td>
                <td>
                    @Html.ActionLink("Edit", "EditStudent", new { id=item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.Id })
                </td>
            </tr>
        }

        </table>*@

</body>
</html>
