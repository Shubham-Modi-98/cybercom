Work Report on HTML and JavaScript [18/01/2021]

• Today, I watched the videos on Javascript on Google Drive link.
• Today, I learn the more Detailed in function Prototype chaining
• Function chaining, In that object function used by diff object like inheritane.
	var shubh = {
	    name: 'Shubham Modi',
	    doy: 1998,
	    age: function() {
	        var curYear = new Date().getFullYear();
	        var curAge = curYear - this.doy;
	        document.writeln(`${this.name} age is ${curAge}.`);
        	console.log(`${this.name} age is ${curAge}.`);
	    }
	}
	//Diff object used diff object funcion
	var teenu = {
	    name: 'Teenu Modi',
	    doy: 1969
	};

	shubh.age();

	teenu.age = shubh.age;
	teenu.age();

• Also, learn for and for in loop and check the difference between them practically
	var a = {x:'ABC'};
	for(var i=0; i<5; i++)
	{
    		console.log(a.x);
	    	console.log(a);

	}
	for (X in a) {
	    document.writeln(a.x);
	    document.writeln(X.a);
	    document.writeln(X);
    	    console.log(a.x);
	    console.log(X.a);
	    console.log(X);
	}

• More detailed of object and it's property ow to access how we used, also implement in Practical.
• Map Method : This method creates a new array with the results of calling a function for every array element and 
		calls the provided function once for each element in an array, in order.
• Arrow Function: This introduced in ES6. By using this we can shorter the syntax
• difference in Array in ES5 and ES6.

• Also learn create registration form with fieldset and legend and used dropdown.
• Then completed HTML Practices given by Pritee Mam.
	
• Prototype Chaining Snippet
	var Person = function(name,doy) {
	    this.name = name;
	    this.doy = doy;
	}
	Person.prototype.calcAge = 
	function() {
	    var curYear = new Date().getFullYear();
	    var curAge = curYear - this.doy;
	    document.writeln(`${this.name} age is ${curAge}.`);
	    console.log(`${this.name} age is ${curAge}.`);
	}

	virat = {
	    name: 'Virat Kohli',
	    doy: 1988
	};

	var vk = new Person(virat.name,virat.doy);
	vk.calcAge();

	var chris = new Person('Chris Hemsworth',1983).calcAge();